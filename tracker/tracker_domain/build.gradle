apply from: "${rootProject.projectDir}/base-module.gradle"

android {
    namespace 'com.doersweb.tracker_domain'
}

dependencies {
    implementation project(Modules.core)
    implementation Coroutines.coroutines
}